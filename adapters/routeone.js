export async function getRouteOneNotes(applicationId, { username, password }) {
  // TODO: Replace stub with real login + scraping or API calls
  return `RouteOne notes for ${applicationId} (stub)`;
}
